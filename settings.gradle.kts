pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenLocal()
    }
}

plugins {
    id("com.possible-triangle.helper") version ("1.1")
    id("com.possible-triangle.packwiz") version ("1.1.60")
}
